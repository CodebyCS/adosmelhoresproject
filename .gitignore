## [ Visual Studio / .NET ]
bin/
obj/
.vs/
*.user
*.userosscache
*.sln.docstates
.devcontainer/

## [ Blazor Specific ]
# Arquivos de log do servidor e cache de compilação do Razor
.razor.log
*.vbdproj

## [ JSON Database - ESTRATÉGIA DE ENGENHARIA ]
# Se vocês querem que cada aluno tenha seus próprios dados de teste sem dar conflito:
# 1. Renomeiem o arquivo oficial para "database.json" e adicionem a linha abaixo.
# 2. Mantenham um "seed_data.json" (modelo vazio) rastreado no Git para os outros.
database.json

# Se decidirem rastrear o JSON (não recomendado por gerar conflitos constantes), 
# apaguem a linha acima, mas preparem-se para muitos "merge conflicts".

## [ Bibliotecas e Dependências ]
libman.json
.librarymanager.json
node_modules/
/wwwroot/lib/

## [ Sistema ]
.DS_Store
Thumbs.db
